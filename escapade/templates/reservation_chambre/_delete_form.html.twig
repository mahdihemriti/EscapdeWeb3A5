<form method="post" action="{{ path('app_reservation_chambre_delete', {'id': reservation_chambre.id}) }}"
      onsubmit="return confirm('Êtes-vous sûr de bien vouloir supprimer cet élément?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ reservation_chambre.id) }}">
    <button class="btn">Supprimer</button>
</form>
