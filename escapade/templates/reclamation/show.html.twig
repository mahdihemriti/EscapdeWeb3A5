{% extends 'baseBack.html.twig' %}

{% block title %}Reclamation{% endblock %}

{% block content %}
    <h1>Reclamation</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Date</th>
                <td>{{ reclamation.date ? reclamation.date|date('Y-m-d') : '' }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ reclamation.description }}</td>
            </tr>
            <tr>
                <th>nom</th>
                <td>{{ reclamation.idclient.nom }}</td>
            </tr>
            <tr>
                <th>prenom</th>
                <td>{{ reclamation.idclient.prenom }}</td>
            </tr>
            <tr>
                <th>email</th>
                <td>{{ reclamation.idclient.email }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_reclamation_index') }}">back to list</a>

    <a href="{{ path('app_reclamation_edit', {'id': reclamation.id}) }}">edit</a>

    {{ include('reclamation/_delete_form.html.twig') }}
{% endblock %}
