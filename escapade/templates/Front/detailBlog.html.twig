{% extends 'base.html.twig' %}

{% block content %}

    <div id="fh5co-tours" class="fh5co-section-gray">

        <div class="container">

            <div class="col-md-4 animate-box">
                <div class="row">
                    <div class="col-md-12">
                        <br>
                        <h4 class="prod-title">{{ blog.nom }}</h4>
                        <p class="posted_by">{{ blog.date ? blog.date|date('Y-m-d') : '' }}</p>
                    </div>

                    <p>{{ blog.description }}</p>
                </div>

            </div>
            <div class=" col-md-6 animate-box" >
                <img class="img-responsive" src="{{ asset('images/' ~ blog.image) }}" alt="travel">
            </div>



        </div>
        <a class="btn btn-primary" href="{{ path('app_blog_indexfront') }}">Retour à la liste</a>
    </div>


{% endblock %}