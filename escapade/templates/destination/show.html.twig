{% extends 'baseBack.html.twig' %}

{% block content %}

    <h1>Destination</h1>
<br>
    <br>
    <table class="table table-striped">
        <tbody>

            <tr>
                <th>PAYS</th>
                <td>{{ dest.pays }}</td>
            </tr>
            <tr>
                <th>VILLE</th>
                <td>{{ dest.ville }}</td>
            </tr>
            <tr>
                <th>IMAGE</th>
                <td>
                    <img src="{{ asset('images/' ~ dest.img) }}" width="50" height="50">
                </td>

            </tr>
        </tbody>
    </table>

         <a class="btn btn-primary " href="{{ path('app_destination_index') }}">Retour à la liste</a>

         <a class="btn btn-primary " href="{{ path('app_destination_edit', {'id': dest.id}) }}">Modifier</a>


    <br>

    <br>

    {{ include('destination/_delete_form.html.twig') }}
{% endblock %}
