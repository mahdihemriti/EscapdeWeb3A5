

<br>
<table class="table table-striped">
    <thead>
    <tr>
        <th>PAYS</th>

        <th>ACTIONS</th>
    </tr>
    </thead>
    <tbody>
    {% for destination in destinations %}
        <tr>
            <td>{{ destination.pays }}</td>



            <td>

                <a class="btn btn-primary " href="{{ path('app_destination_show', {'id': destination.id}) }}"> Détails </a>

                <a class="btn btn-primary " href="{{ path('app_destination_edit', {'id': destination.id}) }}"> Modifier</a>



            </td>
        </tr>
    {% else %}
        <tr>
            <td colspan="5">Aucune destination trouvée</td>
        </tr>
    {% endfor %}
    </tbody>
</table>
<p>
    <a  class="btn btn-primary " href="{{ path('app_destination_new') }}">Ajouter une destination</a>
</p>
<p>
<a  href="{{ path('listo1') }}">Mettre sous forme pdf</a>
</p>