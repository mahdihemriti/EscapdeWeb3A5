{% extends 'baseBack.html.twig' %}



{% block content %}
    <h1>Commentaire</h1>

    <table class="table">
        <tbody>

            <tr>
                <th>Description</th>
                <td>{{ commentaire.description }}</td>
            </tr>
            <tr>
                <th>Date</th>
                <td>{{ commentaire.date ? commentaire.date|date('Y-m-d') : '' }}</td>
            </tr>
            <tr>
                <th>Blog</th>
                <td>{{ commentaire.idblog.nom }}</td>
            </tr>
            <tr>
                <th>Client</th>
                <td>{{ commentaire.idclient.nom }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_commentaire_index') }}">back to list</a>

{% endblock %}
